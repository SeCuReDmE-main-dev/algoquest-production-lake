# Immutable submissions

The importer creates one directory per packet and submission. Original files are ignored by Git; the submission manifest remains the review record. Never overwrite an existing submission.
