# Contrats

Les schémas JSON sous `schemas/` sont normatifs pour cette version : ImageSlot, PromptPacket, AssetSubmission, ProductionJob et ResearchSource.

Les données canoniques utilisent UTF-8. Les registres JSONL contiennent un objet JSON par ligne et se terminent par une nouvelle ligne.
