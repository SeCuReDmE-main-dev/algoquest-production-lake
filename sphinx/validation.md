# Validation

`tools/validate_lake.py` vérifie les comptes, identifiants, chemins, noms de fichiers, dépendances et catalogues. La CI compile MkDocs en mode strict et Sphinx avec les avertissements traités comme erreurs.
